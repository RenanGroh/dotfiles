[global]
    # --- Geometria e Posição ---
    width = (300, 650)
    height = 200
    origin = top-right
    offset = 20x20
    notification_limit = 5
    follow = none
    monitor = DP-3	

    # --- Aparência ---
    font = JetBrains Mono Nerd Font 12
   
    # Arredondamento (Clean)
    corner_radius = 3
    
    # Borda Branca Fina
    frame_width = 1
    frame_color = "#E5E7E6"

    # Espaçamento
    padding = 15
    horizontal_padding = 15
    text_icon_padding = 15

    # Ícones
    icon_position = left
    min_icon_size = 32
    max_icon_size = 200
    enable_recursive_icon_lookup = true

    # --- Comportamento ---
    progress_bar = true
    progress_bar_height = 10
    progress_bar_frame_width = 0
    progress_bar_min_width = 150
    progress_bar_max_width = 300

    # Formato do texto
    markup = full
    format = "<b>%s</b>\n%b"
    alignment = left
    vertical_alignment = center
    show_age_threshold = 60
    ellipsize = middle
    ignore_newline = no
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = yes

[urgency_low]
    # Preto Transparente (CC no final é o Alpha/Transparência)
    background = "#14130122"
    foreground = "#888888"
    frame_color = "#FFFFFF"
    timeout = 5

[urgency_normal]
    # Preto Transparente
    background = "#14130122"
    foreground = "#E5E7E6"
    frame_color = "#FFFFFF"
    timeout = 5 

[urgency_critical]
    # Vermelho Escuro Transparente para erros graves
    background = "#D72A1D22"
    foreground = "#FFCCCC"
    frame_color = "#FF0000"
    timeout = 0
